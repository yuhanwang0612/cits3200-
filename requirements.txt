# CITS3200 — researcher productivity / publication rankings tool
# Install:  pip install -r requirements.txt
 
# --- scraping & HTTP ---
requests>=2.32,<3
beautifulsoup4>=4.12,<5
 
# --- data handling ---
pandas>=2.2,<4
openpyxl>=3.1,<4          # required by pandas to read the ABDC .xlsx
 
# --- database ---
SQLAlchemy>=2.0,<3
 
# --- web app ---
Flask>=3.0,<4
 
python-dotenv>=1.0,<2

# --- browser, for the two directories that render their staff list in JS ---
# Only adapters/unsw.py needs these, and only when its roster cache is cold.
# The imports are inside the function so a run against a warm cache does not
# need them installed at all.
selenium>=4.20,<5
webdriver-manager>=4.0,<5