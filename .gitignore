# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/

# Secrets
.env

# Database (regenerated by load.py)
research.db

# Licensed reference data — ABDC and Scimago both prohibit redistribution,
# so these are downloaded locally, never committed.
*.xlsx
scimagojr*.csv

# OpenAlex response cache — rankings/openalex.py writes this next to a
# publications file so re-runs don't re-hit the API. Regenerated freely.
openalex_cache.json
output/orcid_resolution_cache.json

# Personal working notes — not a project artefact
docs/HANDOFF.md
output/MAIN_INTEGRATION_FINDINGS.md

# Editors / OS
.DS_Store

# Editors / tooling
CLAUDE.md
.claude/
scratch/
output/clarivate_cache.json
output/clarivate_jif.csv

cache/

# Independent UWA/UniMelb pipeline runtime. The committed data/README.md is
# the contract; caches, raw runs, review decisions and published revisions are
# local/regenerable artefacts.
/data/cache/
/data/runs/
/data/failed/
/data/published/
/data/current.json
/data/latest_run.json
/data/review.sqlite3*
/data/refresh.lock
