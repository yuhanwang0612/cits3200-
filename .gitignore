# CITS3200 Group 20

# Cached staff profile HTML — ~350 files, regenerated by re-running the scraper.
# The CSV/JSON outputs below are the actual result and are worth committing.
output/profile_cache/
output/debug_*.html

# Licensed data must never be committed. ABDC is free to download but its terms
# are per-user, and Clarivate JCR data is under a UWA subscription — neither may
# be redistributed via a public repository.
data/licensed/
*_jcr_*.xlsx
*_clarivate_*.csv

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
env/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
~$*
